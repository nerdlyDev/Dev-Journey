// Bash and terminal 

// ternimal is but another interface to the computer

// pwd - print working directory

// cd - change directory

// cd .. - go up one directory

// cd ~ - go to home directory

// mkdir - make directory

// touch - create a file

// rm - remove a file

// rm -r - remove a directory

// ls - list files and 

// ls -a - list all files including hidden files

// ls -l - list files in long format

// ls -t - list files by last modified

// ls -r - list files in reverse order

// ls -R - list files in subdirectories

// ls -lh - list files in long format with readable file size

// ls -lt - list files in long format by last modified

// ls -ltr - list files in long format in reverse order by last modified

// ls -lS - list files in long format by file size

// ls -lSr - list files in long format in reverse order by file size

// ls -lR - list files in long format in subdirectories

// ls -lRt - list files in long format by last modified in subdirectories

// ls -lRtr - list files in long format in reverse order by last modified in subdirectories

// ls -lRS - list files in long format by file size in subdirectories



// cat - concatenate and display files

// cat -n - display line numbers

// cat -b - display line numbers except blank lines

// cat -s - display multiple blank lines as a single blank line

// cat -E - display $ at the end of each line

// cat >> - append to a file

// cat > - overwrite a file

// cat < - read from a file



// cp - copy files and directories

// cp -r - copy directories

// mv - move files and directories

// echo - display a line of text

// vi - open a file in vi editor

// clear - clear the terminal

// grep - search for a pattern in a file

// grep -c - display the count of matching lines

// grep -l - display the names of files with matching lines

// grep -n - display the matching lines and their line numbers

// grep -h - display the matching lines without the file names

// grep -i - search for a pattern in a file ignoring case   

// grep -r - search for a pattern in a directory

// grep -v - search for a pattern in a file and display lines that do not match

// chmod - change file permissions

// chmod 400 - read by owner

// chmod 200 - write by owner

// chmod 100 - execute by owner

// chmod 040 - read by group

// chmod 020 - write by group

// chmod 010 - execute by group

// chmod 004 - read by others

// chmod 002 - write by others

// chmod 001 - execute by others

// chmod 777 - read, write, execute by all

// chmod 755 - read, write, execute by owner, read and execute by others

// chmod 644 - read, write by owner, read by others

// chown - change file owner and group

// echo $PATH - display the path

// head - display the first few lines of a file

// tail - display the last few lines of a file

// wc - display the number of lines, words, and characters in a file

// tail | head - display the middle of a file

// sed - stream editor for filtering and transforming text

// sed 's/old/new/g' - replace all occurrences of old with new

// sed -n - suppress automatic printing of pattern space

// sed -e - add a script to the commands to be executed

// sed -f - add a script file to the commands to be executed

// sed -i - edit files in place

// sed -r - use extended regular expressions in the script

// awk - pattern scanning and processing language

// awk '{print $1}' - display the first column of a file

// awk '{print $1, $2}' - display the first and second columns of a file

// awk -F - specify the field separator

// awk -f - add a script file to the commands to be executed

// awk -v - assign a value to a variable

// awk -i - edit files in place

// ps - display the current processes

//------------------------ Node commands ------------------------------------

// node - run a JavaScript file

// node -v - check the version of node

// npm - Node Package Manager

// nvm - Node Version Manager

// npm init - create a package.json file

// npm install - install a package

// npm install -g - install a package globally

// npm install --save - install a package and save it in the package.json file

//--------------- Bash Adv ------------------------------

// Bash is nothing but a command line interpreter language.

//